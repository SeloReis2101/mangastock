{
    "builds": [{
        "src": "mangastock/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.1.2" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "mangastock/wsgi.py"
        }
    ]
}
